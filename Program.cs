namespace ConversorDeMoedas
{
    internal static class Program
    {
        [STAThread]
        static void Main()
        {
            // AUTO: PEDRO MIGUEL
            // TOTALMENTE FEITO EM C# SEM COPIAS, FEITO 100% POR MIM!
            ApplicationConfiguration.Initialize();
            Application.Run(new CBLTech());
        }
    }
}